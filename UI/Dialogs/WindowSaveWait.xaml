<Window x:Class="OperPage_les.UI.Dialogs.WindowSaveWait"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OperPage_les.UI.Dialogs"
        mc:Ignorable="d"
        Title="Сохранение..." Height="150" Width="450" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" WindowStyle="None" Background="#FF8EC1D6" AllowsTransparency="True" Topmost="True" UseLayoutRounding="True" ShowInTaskbar="False" SnapsToDevicePixels="True">
    <Window.Resources>
        <ResourceDictionary Source="/CORE/Resources/VisualDictionary.xaml"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Border BorderThickness="0,0,0,2" BorderBrush="#FF68AACA">
            <TextBlock TextWrapping="Wrap" Text="Происходит сохранение ваших самых важных данных" FontSize="16" TextAlignment="Center" Padding="0,2,0,2" FontFamily="{DynamicResource Deledda Open Regular}"/>
        </Border>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="TextBlockInfoSaving" Grid.Row="1" Padding="2,0,2,0" LineStackingStrategy="BlockLineHeight" TextTrimming="CharacterEllipsis" Text="Text" Background="#FFB9B6A1"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Image x:Name="ImageIndicator" Grid.Column="1" Stretch="None" Width="26" Height="26" VerticalAlignment="Bottom" Margin="5,0,5,0"/>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock x:Name="TextBlockTime" Grid.Row="1" TextWrapping="Wrap" Text="0" TextAlignment="Right" FontSize="16" FontFamily="{DynamicResource Tablon}"/>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Window>
