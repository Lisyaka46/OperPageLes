<UserControl x:Class="AAC20.GUI.IELButtonText"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AAC20.GUI"
             mc:Ignorable="d" 
             d:DesignHeight="35.181" d:DesignWidth="69.642">
    <Grid>
        <Border x:Name="BorderButton" BorderBrush="Black" BorderThickness="3,3,3,3" CornerRadius="10,10,10,10">
            <Grid x:Name="GridButton">
                <TextBlock x:Name="TextBlockButton" Text="TextBlock" TextAlignment="Center" Foreground="Black" Focusable="True" TextWrapping="Wrap" ScrollViewer.HorizontalScrollBarVisibility="Hidden" ScrollViewer.VerticalScrollBarVisibility="Hidden" VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
